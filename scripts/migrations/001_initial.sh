#!/bin/bash
# Migration 001: Initial data volume layout (v0 -> v1)
# This is a no-op — the current layout is already v1.
# Establishes the versioning system.
echo "[001_initial] Data volume layout is already v1 — no changes needed"
