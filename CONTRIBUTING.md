# Contributing

OpenClaw Machines public core accepts contributions under Apache-2.0.

Use DCO signoff on commits:

```bash
git commit -s
```

Before opening a PR, run the relevant checks from `docs/TESTING.md`.
